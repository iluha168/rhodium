{
  "name": "rhodium",
  "version": "0.1.0",
  "description": "A TypeScript `Promise` wrapper that adds syntax sugar.",
  "homepage": "https://github.com/iluha168/rhodium#readme",
  "bugs": {
    "url": "https://github.com/iluha168/rhodium/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iluha168/rhodium.git"
  },
  "license": "MIT",
  "author": "iluha168",
  "type": "module",
  "main": "dist/mod.mjs",
  "types": "dist/mod.d.mts",
  "scripts": {
    "test": "deno test",

    "fmt": "cd ./test && deno fmt $FMT_OPTIONS && cd ..",
    "fmt:check": "FMT_OPTIONS=--check npm run fmt",
    "fmt:watch": "FMT_OPTIONS=--watch npm run fmt",
    
    "check": "npm run fmt:check && npm run test",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "prepublishOnly": "npm run check && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
